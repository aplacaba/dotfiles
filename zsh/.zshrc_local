# functions
t()
{
  tmux new-session -s $1
}

genpasswd()
{
  local l=$1
    [ "$l" == "" ] && l=20
    tr -dc A-Za-z0-9_ < /dev/urandom | head -c ${l} | xargs
}

drm ()
{
  docker stop $1 && docker rm $1
}

dm-env()
{
  eval $(dm env $2 $1)
}

docker-clean()
{
  docker rm $(docker ps -a -q)
  docker rmi $(docker images -f "dangling=true" -q)
}

docker-remove-untagged()
{
  docker rmi $(docker images | grep "^<none>" | awk "{print $3}")
}

load-native-script()
{
  if [ -f /home/alacaba/.tnsrc ]; then
      source /home/alacaba/.tnsrc
  fi
}

@(){
  echo $1
}

stty -ixon

# vars
export VAGRANT_DEFAULT_PROVIDER=virtualbox
export EDITOR='vim'
export GOPATH="$HOME/go"
export PATH="$HOME/.rbenv/bin:$PATH"
export PATH="$HOME/.rbenv/plugins/ruby-build/bin:$PATH"
export KERN_DIR=/usr/src/kernels/`uname -r`
export PATH="/usr/local/heroku/bin:$PATH"
export PROMPT_COMMAND='echo -ne "\033]0;${USER}@${HOSTNAME}"'
export NVM_DIR="$HOME/.nvm"
export WORKON_HOME="$HOME/Envs"
export ZSH_THEME="sobole"


eval "$(rbenv init -)" # ruby
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"  # This loads nvm
source $HOME/.local/bin/virtualenvwrapper.sh #python

